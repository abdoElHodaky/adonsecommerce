@layout('layouts/main')

@set('title', 'My Account - MultiMarket')

@section('content')
  <div class="flex flex-col md:flex-row gap-6">
    <!-- Sidebar -->
    <div class="md:w-1/4">
      <div class="bg-white rounded-lg shadow-md overflow-hidden sticky top-6">
        <div class="p-6 bg-indigo-600 text-white">
          <h2 class="text-xl font-semibold">My Account</h2>
          <p class="text-sm mt-1">{{ auth.user.firstName }} {{ auth.user.lastName }}</p>
        </div>
        
        <nav class="p-4">
          <ul class="space-y-2">
            <li>
              <a href="{{ route('customer.dashboard') }}" class="flex items-center px-4 py-2 text-indigo-600 bg-indigo-50 rounded-lg">
                <i class="fas fa-tachometer-alt mr-3"></i>
                <span>Dashboard</span>
              </a>
            </li>
            <li>
              <a href="{{ route('customer.orders') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-gray-100 rounded-lg">
                <i class="fas fa-shopping-cart mr-3"></i>
                <span>My Orders</span>
              </a>
            </li>
            <li>
              <a href="{{ route('customer.wishlist') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-gray-100 rounded-lg">
                <i class="fas fa-heart mr-3"></i>
                <span>Wishlist</span>
              </a>
            </li>
            <li>
              <a href="{{ route('customer.reviews') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-gray-100 rounded-lg">
                <i class="fas fa-star mr-3"></i>
                <span>My Reviews</span>
              </a>
            </li>
            <li>
              <a href="{{ route('customer.addresses') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-gray-100 rounded-lg">
                <i class="fas fa-map-marker-alt mr-3"></i>
                <span>Addresses</span>
              </a>
            </li>
            <li>
              <a href="{{ route('customer.profile') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-gray-100 rounded-lg">
                <i class="fas fa-user mr-3"></i>
                <span>Profile</span>
              </a>
            </li>
            <li>
              <a href="{{ route('auth.logout') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-gray-100 rounded-lg">
                <i class="fas fa-sign-out-alt mr-3"></i>
                <span>Logout</span>
              </a>
            </li>
          </ul>
        </nav>
      </div>
    </div>
    
    <!-- Main Content -->
    <div class="md:w-3/4">
      <!-- Welcome Card -->
      <div class="bg-white rounded-lg shadow-md p-6 mb-6">
        <h1 class="text-2xl font-bold mb-2">Welcome back, {{ auth.user.firstName }}!</h1>
        <p class="text-gray-600">Here's a summary of your recent activity and orders.</p>
      </div>
      
      <!-- Stats Cards -->
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-6">
        <div class="bg-white rounded-lg shadow-md p-6">
          <div class="flex items-center">
            <div class="p-3 rounded-full bg-indigo-100 text-indigo-600">
              <i class="fas fa-shopping-cart text-xl"></i>
            </div>
            <div class="ml-4">
              <p class="text-sm text-gray-500">Total Orders</p>
              <h3 class="text-2xl font-semibold">{{ stats.totalOrders }}</h3>
            </div>
          </div>
        </div>
        
        <div class="bg-white rounded-lg shadow-md p-6">
          <div class="flex items-center">
            <div class="p-3 rounded-full bg-green-100 text-green-600">
              <i class="fas fa-box text-xl"></i>
            </div>
            <div class="ml-4">
              <p class="text-sm text-gray-500">Delivered</p>
              <h3 class="text-2xl font-semibold">{{ stats.deliveredOrders }}</h3>
            </div>
          </div>
        </div>
        
        <div class="bg-white rounded-lg shadow-md p-6">
          <div class="flex items-center">
            <div class="p-3 rounded-full bg-yellow-100 text-yellow-600">
              <i class="fas fa-star text-xl"></i>
            </div>
            <div class="ml-4">
              <p class="text-sm text-gray-500">Reviews</p>
              <h3 class="text-2xl font-semibold">{{ stats.totalReviews }}</h3>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Recent Orders -->
      <div class="bg-white rounded-lg shadow-md overflow-hidden mb-6">
        <div class="p-6 border-b border-gray-200">
          <div class="flex justify-between items-center">
            <h2 class="text-lg font-semibold">Recent Orders</h2>
            <a href="{{ route('customer.orders') }}" class="text-sm text-indigo-600 hover:text-indigo-800">View All</a>
          </div>
        </div>
        
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Order ID
                </th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Date
                </th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Status
                </th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Total
                </th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Actions
                </th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              @each(order in recentOrders)
                <tr>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                    #{{ order.orderNumber }}
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    {{ new Date(order.createdAt).toLocaleDateString() }}
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    @if(order.status === 'pending')
                      <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-yellow-100 text-yellow-800">
                        Pending
                      </span>
                    @elseif(order.status === 'processing')
                      <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800">
                        Processing
                      </span>
                    @elseif(order.status === 'shipped')
                      <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-indigo-100 text-indigo-800">
                        Shipped
                      </span>
                    @elseif(order.status === 'delivered')
                      <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                        Delivered
                      </span>
                    @elseif(order.status === 'cancelled')
                      <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-red-100 text-red-800">
                        Cancelled
                      </span>
                    @else
                      <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-800">
                        {{ order.status }}
                      </span>
                    @endif
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    ${{ order.total.toFixed(2) }}
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                    <a href="{{ route('customer.orderDetail', { id: order.id }) }}" class="text-indigo-600 hover:text-indigo-900 mr-3">
                      View
                    </a>
                    @if(order.status === 'delivered')
                      <a href="{{ route('customer.writeReview', { orderId: order.id }) }}" class="text-indigo-600 hover:text-indigo-900">
                        Review
                      </a>
                    @endif
                  </td>
                </tr>
              @else
                <tr>
                  <td colspan="5" class="px-6 py-4 text-center text-sm text-gray-500">
                    No orders found
                  </td>
                </tr>
              @endeach
            </tbody>
          </table>
        </div>
      </div>
      
      <!-- Recently Viewed Products -->
      <div class="bg-white rounded-lg shadow-md overflow-hidden">
        <div class="p-6 border-b border-gray-200">
          <h2 class="text-lg font-semibold">Recently Viewed Products</h2>
        </div>
        
        <div class="p-6">
          @if(recentlyViewedProducts && recentlyViewedProducts.length > 0)
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
              @each(product in recentlyViewedProducts)
                <div class="border border-gray-200 rounded-lg overflow-hidden">
                  <a href="{{ route('store.productDetail', { slug: product.slug }) }}">
                    <div class="h-40 bg-gray-200">
                      @if(product.images && product.images.length > 0)
                        <img src="{{ product.images[0].path }}" alt="{{ product.name }}" class="h-full w-full object-cover">
                      @else
                        <div class="h-full w-full flex items-center justify-center">
                          <i class="fas fa-image text-4xl text-gray-400"></i>
                        </div>
                      @endif
                    </div>
                  </a>
                  
                  <div class="p-4">
                    <a href="{{ route('store.merchantDetail', { slug: product.merchant.slug }) }}" class="text-sm text-indigo-600 hover:text-indigo-800">
                      {{ product.merchant.storeName }}
                    </a>
                    
                    <a href="{{ route('store.productDetail', { slug: product.slug }) }}" class="block mt-2 text-lg font-semibold hover:text-indigo-600">
                      {{ product.name }}
                    </a>
                    
                    <div class="mt-2 flex items-center">
                      <span class="text-gray-900 font-bold">${{ product.price.toFixed(2) }}</span>
                      @if(product.compareAtPrice)
                        <span class="ml-2 text-gray-500 line-through text-sm">${{ product.compareAtPrice.toFixed(2) }}</span>
                      @endif
                    </div>
                  </div>
                </div>
              @endeach
            </div>
          @else
            <div class="text-center py-8">
              <p class="text-gray-500">No recently viewed products</p>
              <a href="{{ route('store.products') }}" class="mt-4 inline-block bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-2 rounded-lg">
                Browse Products
              </a>
            </div>
          @endif
        </div>
      </div>
    </div>
  </div>
@endsection

